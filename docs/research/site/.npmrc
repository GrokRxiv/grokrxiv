; Standalone Next.js app — keep it out of the repo's pnpm workspace and skip
; the implicit pre-run install check. The build is a one-shot operation that
; doesn't need cross-workspace consistency.
ignore-workspace=true
verify-deps-before-run=false
auto-install-peers=true
strict-peer-dependencies=false
