#!/bin/sh
set -eu

case "$0" in
  */*) script_dir=${0%/*} ;;
  *) script_dir=. ;;
esac
script_dir=$(CDPATH= cd -- "$script_dir" && pwd -P)
export PATH="$script_dir:$PATH"

if [ "$#" -eq 0 ]; then
  echo "usage: grokrxiv-corpus-env <command> [args...]" >&2
  exit 64
fi

exec "$@"
