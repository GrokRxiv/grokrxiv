Review the supplied corrected LaTeX/PDF build evidence.

Return strict JSON matching `schema.json`. Mark `status` as `fail` for missing
or non-compiling artifacts, broken review identifiers, malformed LaTeX, or
formatting that would block publication review.
