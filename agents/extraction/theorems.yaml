id: theorem_graph_extractor
role: "Build a dependency graph over theorem-like blocks (theorem/lemma/proposition/corollary/definition/proof). Resolves \\ref{} edges."
provider: gemini
# Keep theorem graph extraction on the local Gemini CLI path by default.
# Math-heavy papers can still override the model/runner explicitly.
model: gemini-2.5-flash
runner: cli
prompt_template: prompts/extraction/theorems.md
output_schema: schemas/extraction/theorems.schema.json
tools:
  - read_file
  - query_ast
  - list_sections
  - read_section
  - resolve_label
  - submit
max_iters: 50
max_cost_usd: 1.50
timeout_secs: 240
